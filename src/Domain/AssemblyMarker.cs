namespace Domain;

/// <summary>
/// Ponto de ancoragem para reflection (testes de arquitetura, DI scanning) — sem lógica própria.
/// Remover se, no futuro, outro tipo público estável do assembly puder cumprir o mesmo papel.
/// </summary>
public sealed class AssemblyMarker;
